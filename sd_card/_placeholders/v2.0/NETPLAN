network:
  version: 2
  wifis:
    wlan0:
      dhcp4: true
      optional: true
      access-points:
        # These are the WiFi networks provided through the sd_card init command
        {netplan_wifi_networks}
